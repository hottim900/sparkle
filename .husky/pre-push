# === Block any ref update targeting main ===
while read -r _local_ref _local_sha remote_ref _remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo ""
    echo "  ERROR: 禁止直接 push 到 main。請 push feature branch 再開 PR。"
    echo ""
    exit 1
  fi
done
