# Sparkle Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

NODE_ENV=production
PORT=3000

# Listen address (default: 127.0.0.1, use 0.0.0.0 to accept external connections)
# HOST=127.0.0.1

# SQLite database path
DATABASE_URL=./data/todo.db

# Authentication token for the web UI (choose a strong random string)
AUTH_TOKEN=your-secret-token-here

# VPN subnet for iptables firewall rules (optional, default: 10.0.0.0/8)
# VPN_SUBNET=10.0.0.0/24

# TLS certificates (optional — omit to run plain HTTP)
# Generate with: mkcert localhost 127.0.0.1 YOUR_LAN_IP
# TLS_CERT=./certs/localhost+1.pem
# TLS_KEY=./certs/localhost+1-key.pem

# LINE Bot (optional — omit to disable LINE integration)
# LINE_CHANNEL_SECRET=from-line-developer-console
# LINE_CHANNEL_ACCESS_TOKEN=from-line-developer-console

# LINE admin notifications — used by monitoring scripts (optional)
# Get your userId from LINE Developer Console or webhook logs
# LINE_ADMIN_USER_ID=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Sentry error tracking (optional — omit to disable)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Offsite backup repository (optional — omit to skip offsite copy)
# After local restic backup, copies snapshots to a second repo for redundancy.
# Initialize with: restic init --repo /mnt/d/sparkle-backups --copy-chunker-params --repo2 ~/sparkle-backups
# RESTIC_OFFSITE_REPOSITORY=/mnt/d/sparkle-backups
# RESTIC_OFFSITE_PASSWORD_FILE=~/.sparkle-backup-password
