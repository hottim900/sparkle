# Sparkle Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

NODE_ENV=production
PORT=3000

# SQLite database path
DATABASE_URL=./data/todo.db

# Authentication token for the web UI (choose a strong random string)
AUTH_TOKEN=your-secret-token-here

# TLS certificates (generated with mkcert, see docs/self-hosting.md)
TLS_CERT=./certs/your-cert.pem
TLS_KEY=./certs/your-key.pem

# LINE Bot (optional — only needed if using LINE Bot integration)
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
