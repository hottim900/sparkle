# Sparkle Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

NODE_ENV=production
PORT=3000

# SQLite database path
DATABASE_URL=./data/todo.db

# Authentication token for the web UI (choose a strong random string)
AUTH_TOKEN=your-secret-token-here

# TLS certificates (optional — omit to run plain HTTP)
# Generate with: mkcert localhost 127.0.0.1 YOUR_LAN_IP
# TLS_CERT=./certs/localhost+1.pem
# TLS_KEY=./certs/localhost+1-key.pem

# LINE Bot (optional — omit to disable LINE integration)
# LINE_CHANNEL_SECRET=from-line-developer-console
# LINE_CHANNEL_ACCESS_TOKEN=from-line-developer-console
