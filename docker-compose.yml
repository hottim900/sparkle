services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN:-changeme}
      - PORT=3000
      - NODE_ENV=production
      - DATABASE_URL=/app/data/todo.db
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
